scalar Date # Custom scalar

type Query {
  cards: [Card!]
  card(id: ID!): Card
}

type Card {
  id: ID!
  description: String!
  createdAt: Date!  
}

type Mutation {
  createCard(description: String!): Card
  deleteCard(id: ID!): Card
}

